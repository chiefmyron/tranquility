<div class="page-head">
	<div class="page-title">
		<h1>
            {{ $heading }}
            @if (isset($subheading))
            <small>{{ $subheading }}</small>
            @endif
            <a class="btn hidden-lg hidden-md" id="toggle-toolbar" href="#toolbar-container">Show actions</a>    
        </h1>
	</div>
</div>