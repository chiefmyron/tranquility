<div class="page-head">
	<div class="page-title">
		<h1>
            {{ $heading }}
            @if (isset($subheading))
            <small>{{ $subheading }}</small>
            @endif
        </h1>
	</div>
</div>
